/**
 * DB Init API - STUB (migrated to Convex)
 */
import { NextResponse } from 'next/server';

export async function POST() {
  return NextResponse.json({ success: true, message: 'Convex needs no init' });
}
