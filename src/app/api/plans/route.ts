/**
 * Plans API - STUB (migrated to Convex)
 */
import { NextRequest, NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ success: false, error: 'Use Convex' });
}

export async function POST() {
  return NextResponse.json({ success: false, error: 'Use Convex' });
}
